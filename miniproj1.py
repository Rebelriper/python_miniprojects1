from plyer import notification
notification.notify(title="Reminder!",
                    message="Time to Code babyyy:-)",
                    app_icon=None,
                    app_name="notifier",
                    timeout=8)